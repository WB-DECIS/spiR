% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-inventory-cache.R
\name{.spi_get_inventory}
\alias{.spi_get_inventory}
\title{Internal: resolve the inventory tree for a given SPI version}
\usage{
.spi_get_inventory(version = "master")
}
\arguments{
\item{version}{Character. Branch name. Default \code{"master"}.}
}
\value{
A \code{data.table} with columns \code{path}, \code{type}, \code{size}, \code{category},
\code{pillar}, \code{dimension}.
}
\description{
Returns the on-disk cached tree when valid and fresh. Falls back to
crawling the GitHub repository when the cache is absent, expired, or
corrupt. Errors clearly when neither cache nor network is available.
}
\details{
This is the single entry point for all inventory consumers (e.g.
\code{spi_inventory()}, \code{spi_get_raw()} — planned features).
}
\keyword{internal}
