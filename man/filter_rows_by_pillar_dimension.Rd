% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-filters.R
\name{filter_rows_by_pillar_dimension}
\alias{filter_rows_by_pillar_dimension}
\title{Filter long aggregates data.table rows by pillar and/or dimension}
\usage{
filter_rows_by_pillar_dimension(dt, pillar, dimension)
}
\arguments{
\item{dt}{A \code{data.table} (long format, from
\code{SPI_databank_country_and_aggregates.csv}).}

\item{pillar}{Integer 1–5, or \code{NULL}. Matches rows where \code{source_id}
starts with \verb{SPI.D\{pillar\}.} (individual indicators). Note: pillar-level
summary rows (e.g. \code{SPI.INDEX.PIL1}) are \strong{not} matched by this filter;
to retrieve those, omit \code{pillar} and filter on \code{source_id} manually.}

\item{dimension}{Character string in \code{"P.D"} format, or \code{NULL}.}
}
\value{
A filtered \code{data.table}.
}
\description{
Filters the \code{source_id} column in the aggregates long-format data.table.
When both \code{pillar} and \code{dimension} are provided, \code{dimension} (more
specific) takes precedence.
}
\keyword{internal}
