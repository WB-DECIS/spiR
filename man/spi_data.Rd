% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-wrappers.R
\name{spi_data}
\alias{spi_data}
\title{Retrieve SPI indicator data}
\usage{
spi_data(
  version = "master",
  country = NULL,
  year = NULL,
  pillar = NULL,
  dimension = NULL
)
}
\arguments{
\item{version}{Character scalar naming the SPI data branch. Defaults to
\code{"master"}.}

\item{country}{Character vector of ISO 3166-1 alpha-3 country codes (e.g.
\code{c("NOR", "SWE")}). \code{NULL} returns all countries.}

\item{year}{Numeric or integer vector of years. \code{NULL} returns all years.}

\item{pillar}{Optional pillar filter. Accepts a pillar number or SPI pillar
ID. \code{NULL} returns all pillars.}

\item{dimension}{Optional dimension filter. Accepts a dimension code or SPI
dimension ID. \code{NULL} returns all dimensions.}
}
\value{
A \code{data.table} in wide format with one row per country-year.
Columns include \code{iso3c}, \code{date}, all \verb{SPI.D*}/\verb{RAW.D*} indicator
columns, and country metadata.
}
\description{
A convenience wrapper around \code{spi_get("data", ...)} that retrieves
\code{SPI_data.csv} — individual indicator scores per country-year in wide
format.
}
\examples{
\dontrun{
# All countries, all years
spi_data()

# Single country, multiple years
spi_data(country = "NOR", year = 2020:2024)

# Only Pillar 3 (Data Products) columns
spi_data(pillar = 3)

# Specific dimension
spi_data(dimension = "4.1")
}

}
\seealso{
\code{\link[=spi_index]{spi_index()}}, \code{\link[=spi_aggregates]{spi_aggregates()}}, \code{\link[=spi_get]{spi_get()}}, \code{\link[=spi_versions]{spi_versions()}}
}
