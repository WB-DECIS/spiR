% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-inventory-cache.R
\name{spi_update_inventory}
\alias{spi_update_inventory}
\title{Update the local SPI inventory cache}
\usage{
spi_update_inventory(version = "master")
}
\arguments{
\item{version}{Character. Branch name in the SPI repository. Defaults to
\code{"master"} (latest stable). Use \code{\link[=spi_versions]{spi_versions()}} to list all available
branches.}
}
\value{
The updated inventory \code{data.table}, invisibly. Each row
represents one file in the SPI repository, with columns:
\describe{
\item{\code{path}}{Relative file path within the repository.}
\item{\code{type}}{GitHub object type: \code{"blob"} (file) or \code{"tree"}
(directory).}
\item{\code{size}}{File size in bytes (\code{NA} for directories).}
\item{\code{category}}{\code{"raw"}, \code{"output"}, or \code{"misc"}.}
\item{\code{pillar}}{Integer 1–5, or \code{NA}.}
\item{\code{dimension}}{Character \code{"P.D"} (e.g. \code{"4.1"}), or \code{NA}.}
}
}
\description{
Forces a fresh crawl of the
\href{https://github.com/worldbank/SPI}{World Bank SPI repository} for the
specified version and writes the result to the on-disk inventory cache.
Useful when the remote repository has been updated and you want to
refresh before the automatic 30-day TTL expires.
}
\details{
For reproducible workflows, call \code{spi_update_inventory()} explicitly
before distributing analysis code rather than relying on the automatic
30-day TTL refresh of the internal cache.
}
\section{Development status}{

This function depends on the \code{github-tree-crawler} milestone. Calling
it on the current development build will error with class
\code{"spi_not_implemented"}.
}

\examples{
\dontrun{
# Refresh the default version
spi_update_inventory()

# Refresh a specific version
spi_update_inventory(version = "SPI2023")
}

}
\seealso{
\code{\link[=spi_clear_inventory]{spi_clear_inventory()}}, \code{\link[=spi_versions]{spi_versions()}}

Other spi-inventory-cache:
\code{\link[=spi_clear_inventory]{spi_clear_inventory()}}
}
\concept{spi-inventory-cache}
