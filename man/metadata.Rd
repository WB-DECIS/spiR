% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-wrappers.R
\name{metadata}
\alias{metadata}
\title{Retrieve SPI metadata hierarchy}
\usage{
metadata(pillar = NULL, dimension = NULL, indicator = NULL, version = "master")
}
\arguments{
\item{pillar}{Character scalar, e.g. \code{"1"}. \code{NULL} means no pillar filter.}

\item{dimension}{Character scalar in \code{"P.D"} format (e.g. \code{"2.1"}).
\code{NULL} means no dimension filter.}

\item{indicator}{Character scalar indicator code (e.g. \code{"SPI.D1.5.POV"}).
\code{NULL} means no indicator filter.}

\item{version}{Character. Branch name in the SPI repository. Defaults to
\code{"master"}.}
}
\value{
A named list with three \code{data.table} elements: \code{pillars},
\code{dimensions}, and \code{indicators}.
}
\description{
Downloads SPI metadata and returns a standardized list with pillar,
dimension, and indicator tables. Optional filters can be supplied as strings
and must be hierarchically consistent.
}
\examples{
\dontrun{
metadata(pillar = "1")
metadata(dimension = "2.1")
metadata(indicator = "SPI.D1.5.POV")
}
}
\seealso{
\code{\link[=metadata_pillars]{metadata_pillars()}}, \code{\link[=metadata_dimensions]{metadata_dimensions()}}, \code{\link[=spi_data]{spi_data()}},
\code{\link[=spi_indicator]{spi_indicator()}}
}
