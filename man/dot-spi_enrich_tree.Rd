% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-filters.R
\name{.spi_enrich_tree}
\alias{.spi_enrich_tree}
\title{Enrich a raw file-tree data.table with pillar, dimension, and category}
\usage{
.spi_enrich_tree(tree_dt)
}
\arguments{
\item{tree_dt}{A \code{data.table} with at minimum a character \code{path} column.}
}
\value{
A \code{data.table} with six columns: \code{path}, \code{type}, \code{size},
\code{category}, \code{pillar}, \code{dimension}.
}
\description{
Takes a raw \code{data.table} (from the GitHub tree crawler) with at minimum a
\code{path} column and adds three metadata columns derived from each file path:
}
\details{
\itemize{
\item \code{category}: \code{"raw"} for files under \verb{01_raw_data/}, \code{"output"} for
files under \verb{03_output_data/}, and \code{"misc"} for everything else.
\item \code{pillar}: integer 1\u20135 extracted from the first subfolder name under
\verb{01_raw_data/} (e.g. \verb{4.1_SOCS} \u2192 \code{4L}). \code{NA} for all other paths.
\item \code{dimension}: character in \code{"P.D"} format from the same subfolder
(e.g. \verb{4.1_SOCS} \u2192 \code{"4.1"}). \code{NA} when the subfolder encodes only a
pillar (e.g. \verb{3_DP}) or the path falls outside \verb{01_raw_data/}.
}

The function is pure: it copies the input before enriching, so the
original \code{data.table} is never modified.
}
\keyword{internal}
