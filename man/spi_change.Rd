% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-change.R
\name{spi_change}
\alias{spi_change}
\title{Add changes from the previous and first valid data years}
\usage{
spi_change(
  data,
  value_col = "SPI.INDEX",
  group_cols = "iso3c",
  year_col = "date"
)
}
\arguments{
\item{data}{A data.frame or data.table containing SPI scores.}

\item{value_col}{Character scalar naming the score column. Defaults to
\code{"SPI.INDEX"}.}

\item{group_cols}{Character vector naming columns that identify a series.
Defaults to \code{"iso3c"}.}

\item{year_col}{Character scalar naming the year column. Defaults to
\code{"date"}.}
}
\value{
A data.table with the input columns and \code{change_previous} and
\code{change_first} columns. The input object is not modified.
}
\description{
Calculates within-group score changes without assuming that every calendar
year is present. Missing scores do not become observations, and repeated
rows for a group-year receive the same change values.
}
\examples{
\dontrun{
scores <- spi_index(country = "KEN")
spi_change(scores)
}
}
\seealso{
\code{\link[=spi_index]{spi_index()}}, \code{\link[=spi_plot_trend]{spi_plot_trend()}}
}
