% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-wrappers.R
\name{spi_aggregates}
\alias{spi_aggregates}
\title{Retrieve SPI regional aggregate scores}
\usage{
spi_aggregates(
  version = "master",
  region = NULL,
  year = NULL,
  pillar = NULL,
  dimension = NULL
)
}
\arguments{
\item{version}{Character. Branch name in the SPI repository. Defaults to
\code{"master"} (the latest stable version). Use \code{\link[=spi_versions]{spi_versions()}} to list
available branches.}

\item{region}{Character vector of region names (e.g.
\code{"Africa Eastern and Southern"}). \code{NULL} returns all regions.}

\item{year}{Integer vector of years (e.g. \code{2023:2024}). Applies to all
types. \code{NULL} returns all years.}

\item{pillar}{Integer (1-5). Restricts indicator columns to the specified
SPI pillar. For \code{"data"} and \code{"index"}: column subsetting. For
\code{"aggregates"}: row filtering on \code{source_id}. \code{NULL} returns all
pillars. Ignored when \code{dimension} is also supplied.}

\item{dimension}{Character in \code{"P.D"} format (e.g. \code{"5.2"}, \code{"1.5"}).
Restricts to a specific dimension. More specific than \code{pillar}; when
both are supplied, \code{dimension} takes precedence. \code{NULL} returns all
dimensions.}
}
\value{
A \code{data.table} in long format. Columns: \code{iso3c}, \code{country}
(region name), \code{date}, \code{source_id}, \code{source_name}, \code{N}, \code{N_obs},
\code{value}, \code{footnote}.
}
\description{
A convenience wrapper around \code{spi_get("aggregates", ...)} that retrieves
\code{SPI_databank_country_and_aggregates.csv} filtered to regional aggregates
only (individual countries are excluded). The result is in long format
with one row per region-year-indicator.
}
\examples{
\dontrun{
# All regions, all years
spi_aggregates()

# One region, Pillar 1 indicators
spi_aggregates(region = "Africa Eastern and Southern", pillar = 1)

# All regions, specific year range, specific dimension
spi_aggregates(year = 2020:2024, dimension = "5.2")
}

}
\seealso{
\code{\link[=spi_data]{spi_data()}}, \code{\link[=spi_index]{spi_index()}}, \code{\link[=spi_get]{spi_get()}}, \code{\link[=spi_versions]{spi_versions()}}
}
