% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-wrappers.R
\name{country_info}
\alias{country_info}
\title{Retrieve SPI country-year metadata}
\usage{
country_info(version = "master", country = NULL, year = NULL)
}
\arguments{
\item{version}{Character. Branch name in the SPI repository. Defaults
to \code{"master"}.}

\item{country}{Character vector of ISO 3166-1 alpha-3 country codes.
\code{NULL} returns all countries.}

\item{year}{Numeric or integer vector of years. \code{NULL} returns all years.}
}
\value{
A \code{data.table} containing \code{date} and country metadata columns:
\code{iso3c}, \code{iso2c}, \code{country}, \code{capital_city}, \code{longitude}, \code{latitude},
region, administrative region, income level, lending type, and
population fields.
}
\description{
A convenience wrapper around \code{spi_get("data", ...)} that returns only
country metadata columns from \code{SPI_data.csv}. The result preserves one row
per country-year because metadata such as income level and population can
change over time.
}
\examples{
\dontrun{
country_info(country = "CHL", year = 2024)
country_info(year = 2020:2024)
}
}
\seealso{
\code{\link[=spi_data]{spi_data()}}, \code{\link[=spi_get]{spi_get()}}, \code{\link[=spi_versions]{spi_versions()}}
}
