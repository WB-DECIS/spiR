% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-metadata.R
\name{metadata_indicators}
\alias{metadata_indicators}
\title{Retrieve SPI indicator metadata}
\usage{
metadata_indicators(
  pillar = NULL,
  dimension = NULL,
  indicator = NULL,
  version = "master"
)
}
\arguments{
\item{pillar}{Character scalar pillar filter. Accepts either the canonical
pillar value (e.g. \code{"2"}) or the SPI pillar ID (e.g.
\code{"SPI.INDEX.PIL2"}), or \code{NULL}.}

\item{dimension}{Character scalar dimension filter. Accepts either the
canonical \code{"P.D"} form (e.g. \code{"2.1"}) or the SPI dimension ID (e.g.
\code{"SPI.DIM2.1.INDEX"}), or \code{NULL}.}

\item{indicator}{Character scalar indicator filter. Accepts the canonical
SPI indicator code (e.g. \code{"SPI.D1.5.POV"}), or \code{NULL}.}

\item{version}{Character. Branch name in SPI repository. Defaults to
\code{"master"}.}
}
\value{
A \code{data.table} with indicator-level metadata. This function returns
metadata definitions (names, descriptions, scoring, IDs), not country-year
indicator values; for data values see \code{\link[=spi_indicator]{spi_indicator()}}.
}
\description{
Convenience wrapper that returns the indicator block from \code{\link[=metadata]{metadata()}}.
}
\examples{
\dontrun{
metadata_indicators()
metadata_indicators(pillar = "2")
metadata_indicators(dimension = "2.1")
metadata_indicators(indicator = "SPI.D2.1.GDDS")
}
}
\seealso{
\code{\link[=metadata]{metadata()}}, \code{\link[=metadata_pillars]{metadata_pillars()}}, \code{\link[=metadata_dimensions]{metadata_dimensions()}},
\code{\link[=spi_indicator]{spi_indicator()}}
}
