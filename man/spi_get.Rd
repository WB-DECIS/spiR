% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-data.R
\name{spi_get}
\alias{spi_get}
\title{Retrieve SPI output data from GitHub}
\usage{
spi_get(
  type = "data",
  version = "master",
  country = NULL,
  year = NULL,
  pillar = NULL,
  dimension = NULL,
  region = NULL
)
}
\arguments{
\item{type}{Character. Which dataset to retrieve:
\itemize{
\item \code{"data"} — \code{SPI_data.csv}: individual indicator scores per
country-year (wide format).
\item \code{"index"} — \code{SPI_index.csv}: pillar and overall SPI index scores
per country-year (wide format).
\item \code{"aggregates"} — regional aggregate scores (long format, regions
only — individual countries are excluded).
}}

\item{version}{Character. Branch name in the SPI repository. Defaults to
\code{"master"} (the latest stable version). Use \code{\link[=spi_versions]{spi_versions()}} to list
available branches.}

\item{country}{Character vector of ISO 3166-1 alpha-3 country codes (e.g.
\code{c("NOR", "SWE")}). Applies to \code{type = "data"} and \code{type = "index"}
only. \code{NULL} returns all countries.}

\item{year}{Integer vector of years (e.g. \code{2023:2024}). Applies to all
types. \code{NULL} returns all years.}

\item{pillar}{Integer (1-5). Restricts indicator columns to the specified
SPI pillar. For \code{"data"} and \code{"index"}: column subsetting. For
\code{"aggregates"}: row filtering on \code{source_id}. \code{NULL} returns all
pillars. Ignored when \code{dimension} is also supplied.}

\item{dimension}{Character in \code{"P.D"} format (e.g. \code{"5.2"}, \code{"1.5"}).
Restricts to a specific dimension. More specific than \code{pillar}; when
both are supplied, \code{dimension} takes precedence. \code{NULL} returns all
dimensions.}

\item{region}{Character vector of region names (e.g.
\code{"Africa Eastern and Southern"}). Applies to \code{type = "aggregates"}
only. \code{NULL} returns all regions.}
}
\value{
A \code{data.table}. For \code{"data"} and \code{"index"}: wide format with
one row per country-year. For \code{"aggregates"}: long format with one row
per region-year-indicator.
}
\description{
Downloads one of the three core SPI output datasets from the
\href{https://github.com/worldbank/SPI}{World Bank SPI GitHub repository} and
optionally filters the result. For a more focused API, see the
convenience wrappers \code{\link[=spi_data]{spi_data()}}, \code{\link[=spi_index]{spi_index()}}, and
\code{\link[=spi_aggregates]{spi_aggregates()}}.
}
\examples{
\dontrun{
# Full SPI data for all countries
spi_get("data")

# Norway and Sweden, year 2024, Pillar 3 columns only
spi_get("data", country = c("NOR", "SWE"), year = 2024, pillar = 3)

# SPI index, dimension 5.2 only
spi_get("index", dimension = "5.2")

# Regional aggregates for Africa, Pillar 1
spi_get("aggregates",
  region = "Africa Eastern and Southern",
  pillar = 1
)

# Use a specific version (branch)
spi_get("data", version = "SPI2023")
}

}
\seealso{
\code{\link[=spi_data]{spi_data()}}, \code{\link[=spi_index]{spi_index()}}, \code{\link[=spi_aggregates]{spi_aggregates()}},
\code{\link[=spi_versions]{spi_versions()}}
}
