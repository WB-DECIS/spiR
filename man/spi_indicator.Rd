% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spi-wrappers.R
\name{spi_indicator}
\alias{spi_indicator}
\title{Retrieve specific SPI indicator columns}
\usage{
spi_indicator(
  indicator,
  version = "master",
  country = NULL,
  year = NULL,
  include_raw = FALSE
)
}
\arguments{
\item{indicator}{Character vector of SPI indicator column names,
e.g. \code{"SPI.D1.5.POV"}.}

\item{version}{Character. Branch name in the SPI repository. Defaults
to \code{"master"}.}

\item{country}{Character vector of ISO 3166-1 alpha-3 country codes.
\code{NULL} returns all countries.}

\item{year}{Numeric or integer vector of years. \code{NULL} returns all years.}

\item{include_raw}{Logical scalar. If \code{TRUE}, also returns corresponding
\code{RAW.D...} columns for the requested indicators when available.}
}
\value{
A \code{data.table} containing identifier/metadata columns plus the
requested \code{SPI.D...} indicator columns (and optional raw columns).
}
\description{
A convenience wrapper around \code{spi_get("data", ...)} that returns only
requested SPI indicator columns from \code{SPI_data.csv}.
}
\examples{
\dontrun{
spi_indicator("SPI.D1.5.POV", country = "CHL", year = 2024)
spi_indicator(c("SPI.D1.5.POV", "SPI.D2.1.GDDS"), include_raw = TRUE)
}
}
\seealso{
\code{\link[=spi_data]{spi_data()}}, \code{\link[=spi_get]{spi_get()}}, \code{\link[=spi_versions]{spi_versions()}}
}
